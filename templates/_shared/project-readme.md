# {{project_name}}

> 模板使用入口：[STD 主说明与执行流程](../../README.md)。实例化后删除本行，保留下方项目 STD 入口。

<!-- 用一小段说明项目解决什么问题、面向谁以及主要交付物；不要把编写指令留在成品中。 -->

<a id="std-entry"></a>

## 工程文档标准：STD

本项目采用 STD `{{adopted_std_version}}`，固定来源见 [std.lock.json](docs/std.lock.json)。
STD 提供工程文档模板、编写规范、AI 编写指南和校验工具，不代替本项目的技术决定。

**作者与 Agent 入口：** [STD 主说明与执行流程](https://github.com/corezilla/STD/blob/{{std_source_revision}}/README.md)。
编写或修改文档前，先读此入口，再按任务选择已采用的模板、通用 AI 指南及专项指南。
文档封面的 STD 链接回到本节；采用记录与来源不可用或有冲突时，报告缺口，不假定遵循最新版。

- 依据项目事实、约束和已有决定设计，交付实际方案、图、接口及适用的验证内容，不只填写职责表。
- 日常修改不检查或自动跟随最新 STD/模板；只有用户明确要求升级时才对齐新版本。
- 结构校验通过不等于设计质量通过，也不代表实现完成或运行验证通过。
- STD 不自动授权提交、push、发布或部署；遵循当前任务授权。

<!-- 实例化说明（完成后删除）：将版本及完整 40 位 std_source_revision 替换为与 lock 一致的值。
也可使用随项目保存的同一 revision 的 STD README 相对链接。不要链接 main/latest 或本机专有路径。
本片段须位于项目简介之后、目录及安装/使用说明之前，保留 std-entry 锚点供文档封面引用。
这是项目入口脚手架，不是产品设计文档；不创建文档 sidecar，也不为使用模板伪造采用决定。
模板自身的 STD 说明见 ../../README.md；正式项目使用上方已锁定来源入口。 -->

## 当前范围与状态

<!-- 说明当前可用能力、计划内容与主要限制；区分设计、实现和已验证事实。 -->

## 开发与调试环境

<!-- 编写建议：本节要让首次接手的作者或Agent在不依赖旧会话的情况下，确定在哪台机器、哪个目录、
用什么工具和命令工作，以及哪些操作不能执行。只填写实际环境；单机项目写清同机承担哪些职责，
不为模板虚构远端或测试机。README保留足够接手的摘要，复杂部署和调试步骤链接项目既有文档，
脚本或配置是执行细节的唯一维护位置，不在多个文档中复制一套易漂移的命令。
每次环境变更同步本节。新Agent先做授权范围内的只读环境检查；记录与实测不符时报告差异，
不要自行重装工具、修改共享环境或把测试环境切换到生产环境。以下说明和空表须以项目事实填写，
编写指令不进入正式项目正文。 -->

### 机器、访问与工作目录

<!-- 分别说明编辑、构建、运行、调试和测试的位置；同机可合并。记录主机别名、适用用户或访问
方式、实际工作目录、被测版本确认方法，以及单人/共享/生产等环境属性。私有地址和账号仅在允许
的仓库可见范围内记录；公开README使用逻辑环境名和受控访问文档。密码、token、私钥和含凭据的
连接串不得写入；只引用已有安全获取方式。没有访问条件应报告缺口，不猜测账号或扫描其他主机。 -->

| 环境/主机别名 | 职责与共享属性 | 访问方式/受控说明 | 项目工作目录 | 分支/commit确认入口 | 维护责任人 / 最后核实日期 |
|---|---|---|---|---|---|

### 工具链、依赖与环境检查

<!-- 写明实际操作系统/架构、编译器、运行时、SDK、包管理器、虚拟环境及外部依赖。版本以已有
锁文件、镜像摘要或工具配置为准，在这里给出入口和核对命令；不要另造第二份版本清单。
必要时说明硬件、驱动、代理或离线条件、环境变量名称及来源，但不记录secret值。
环境检查必须有预期输出或可判定条件，不能仅写“配置正确”。缺少依赖时说明获准的准备流程。 -->

| 工具/依赖 | 版本或配置的唯一来源 | 所属环境 / 激活方式 | 只读检查命令及执行目录 | 预期结果 / 不满足时处理 |
|---|---|---|---|---|

### 常用开发与调试操作

<!-- 为适用的环境准备、构建、启动、停止、调试、测试、复位和清理分别填写真实入口；不适用的
操作说明原因。每条命令都标明执行主机和工作目录、必要前提、目标对象、成功条件及输出位置。
优先引用已有脚本，不新增平行启动/配置路径。调试说明实际attach/launch方式、进程选择与端口，
并解释是否暂停或修改被测服务；停止/清理必须限制到本任务的实例或数据，不能笼统杀进程或删目录。
命令示例存在不等于授权执行；初始化、部署、复位和破坏性操作须符合当前任务授权。 -->

| 操作 | 执行环境 / 工作目录 | 前置条件 / 目标对象 | 命令或操作文档入口 | 成功条件 / 输出位置 | 副作用 / 所需确认 |
|---|---|---|---|---|---|

### 日志、结果与故障定位

<!-- 指明日志查询入口、服务/进程识别、端口归属、调试器、健康检查以及测试报告和运行证据位置。
写出常见故障的可观察现象、先查哪里和安全处理入口，不把“重启试试”作为万能办法。
测试报告跟随测试类型，例如tests/system/reports/<run-id>/、tests/integration/reports/<run-id>/
及tests/unit/<module-id>/reports/<run-id>/；按项目实际共置路径填写，不复制报告。
外部证据使用稳定入口并说明访问条件，敏感日志须脱敏，不把临时本机路径当作团队共享证据。 -->

| 现象 / 检查对象 | 查询命令或诊断入口 | 日志/报告/证据位置 | 正常判据 / 安全处理或升级联系 |
|---|---|---|---|

### 操作边界与已知限制

<!-- 明确可修改和不可修改的环境/资源、共享设备预约及并发隔离方式、账号权限边界，以及部署、
停止公共服务、复位、清理数据等操作的确认要求。说明任务退出时应释放什么、保留哪些证据。
已有环境问题记录影响、临时获准做法、Owner和状态；尚未核实的事实明确标记，不用旧会话记忆
或过期README猜测当前环境。记录本身不授予新的访问、管理或破坏性操作权限。 -->

| 环境/资源或已知问题 | 允许范围 / 禁止事项 / 当前影响 | 操作前确认或协作入口 | 退出清理 / 证据保留 | Owner / 状态 / 核实日期 |
|---|---|---|---|---|

## 快速开始

<!-- 引用上方环境和操作入口，给出一条最小可用路径及预期结果，不重复维护环境配置和命令。
若尚不可运行，明确说明阻碍及已可开展的工作。 -->

## 文档导航

<!-- 链接实际存在的需求、系统设计、下级设计、接口及用户/运维文档；不要预造不存在的链接。 -->

## 开发与验证

<!-- 引用上方构建和测试入口，说明日常变更需要的检查、覆盖范围和未覆盖项，链接测试规格与报告。
不在本节重复维护另一套环境参数。 -->

## 贡献与维护

<!-- 记录实际维护责任、问题反馈、变更和评审入口；按项目情况填写许可与保密边界。 -->
