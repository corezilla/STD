# STD 数据结构与接口描述格式：评审及修改 HANDOFF

- 日期：2026-09-24
- STD 基线：`dbcf87a39aedbb6fb7c0f193216a4cb0f38e7bb7`（`0.1.0-draft.31`）
- 输入：[LLMTier 提案](/Users/ben/work/LLMTier/docs/99_reference/handoff-std-data-interface-format.md)及其引用的 `libdiag-design.md`
- 状态：**draft.31 基线上的修改建议**；后续 STD draft.32 实施见 `docs/design-data-interface-format.md`。本文件保留评审时点，不代表项目自动采用新格式。

## 1. 结论

采纳提案的目标：数据对象和操作应有稳定、可阅读、可追溯的逐项记录，消费者应能据此构造调用和测试向量。LLMTier 的 `libdiag-design.md` 已示范固定字段段落对发现可空性等遗漏的价值。

**不原样采纳“所有模板一律四段数据、六段接口”**。STD 已规定机器契约拥有结构、类型、签名和编码，正文是同一基线的阅读视图与行为解释；`design.data-dictionary` 不能因文档类型而自动取代 Schema/IDL/DDL 等字段权威。机制 §14.3 是交接分配，不应重写 §5 的完整签名。硬件、FPGA、系统级目录与软件内部函数也不能共用带“数据库”栏的接口格式。

最终做法是：**一份共享的“数据记录/操作记录”写法规范 + 按 authority、层级和领域选择的字段集**。记录型内容用固定字段段落；多对象比较、状态转换和覆盖矩阵仍用短表。复用对象仅在唯一来源定义，其他地方用稳定 ID、基线和必要投影引用。系统对外错误码及其错误载荷属于**公共数据结构**，必须在系统级数据章节集中登记并向接口、子系统、模块、实现和验证纵向承接，不得散落在各接口描述中才临时定义。此决定与现有 [`docs/design-writing-guide.md`](docs/design-writing-guide.md) 的记录/矩阵分工，以及 [`docs/interface-data-mapping-standard.md`](docs/interface-data-mapping-standard.md) 的机器来源规则一致。

## 2. 对原提案的逐项裁决

| 提案 | 裁决 | 修改后要求 |
|---|---|---|
| F1 四段数据格式 | **有条件采纳** | `定义/字段/不变量/来源`可作阅读骨架，但字段还须按适用性容纳默认与空值、编码/单位/长度/计数、键与跨字段条件、生产消费、所有权/寿命、状态/版本和验证引用。仅四个标题存在不等于规格完整。|
| F2 六段接口格式 | **部分采纳** | `功能/输入/输出/返回与错误`是核心；必须补提供/消费方、同步/异步、前置/校验顺序、身份权限、副作用、生效/完成、超时/取消/重复及结果后的合法动作。`统计·日志`和`数据库`改为**适用的行为/实现扩展**，不可强制每个接口写空段。|
| F3 分离与复用 | **采纳并限定** | 结构定义与操作语义逻辑分开，但不强制在每份文档新建两套章节。机器源优先拥有字段；文档引用同一成员 ID/selector/版本/hash，完整阅读视图只能从该源生成或逐项核对。|
| F4 硬件/FPGA及契约变体 | **采纳领域变体，拒绝简单换两栏** | 板级/RTL 必须按实际给方向、位宽、时钟/复位、时序、电气、端序/对齐等适用约束；标准接口绑定标准号/版本/项目选项。契约文档继续维护幂等、并发、事务、兼容与弃用，不用六段格式覆盖原有完整规则。|
| D1 数据字典为字段权威 | **不按原文采纳** | 数据字典可维护业务语义、字典关系和经核对的完整字段阅读视图；字段类型/编码的唯一权威仍由项目固定的 Schema/IDL/DDL/ABI 等来源决定。若项目明确批准数据字典本身为某类纯文档定义的权威，应登记其范围，不能与机器源双重维护。|
| D2–D6 模板分层 | **采纳分层，修正重复落点** | 模块 §6 与 §9 分清私有/共享数据；§5.3 只记录文件间交接及所引用的 §9 成员，不把同一方法再完整写一遍。机制 §14.3 只引用 §5。系统/子系统写其拥有的边界决策；是否字段级取决于 authority 和下游需要，不只取决于系统大小。|
| D7 契约模板 | **采纳** | 在原有章节内提供完整机器源阅读视图、操作行为、错误和版本规则；数据库物理表列不作为公共契约的默认字段。|
| V1 机械校验 | **修正后采纳** | 先校验真实成员关联、必需语义槽位、来源绑定和不重复；只做可判定的结构检查。不以数标题或`｜`分隔符数量代替内容评审，不把`N/A`自动算通过。|
| V2 共享指南 | **采纳** | 建议新增 `docs/design-data-interface-format.md` 作为唯一规范；AI 指南引用它，模板保留本层填写入口。不要在多个指南复制完整规则。|
| V3 迁移 | **采纳** | 新发布快照才适用；旧项目按锁定 STD/模板继续工作，用户明确要求升级后才做差异评估。不要“推荐”阶段就令新旧检查器给出矛盾结论。|
| V4 LLMTier 示例 | **作为评审证据，不直接复制** | 可链接已授权的固定 revision 作为实证；STD 官方示例应另写去业务化、可核对机器源的虚构案例。收录 LLMTier 文件须先确认其项目授权、保密范围和不可变来源。|

## 3. 应新增的共享写法规范

建议落在 `docs/design-data-interface-format.md`，由 [`docs/interface-data-mapping-standard.md`](docs/interface-data-mapping-standard.md) 链接。新增规范不得另建接口 catalog 或第二套文档身份。

### 3.1 数据对象记录

每个**由本文拥有或必须完整呈现**的数据对象，至少记录：

1. 稳定 Data/Type ID、名称、用途、适用层级，以及**唯一来源**（文件、selector、版本/revision/hash；Proposed 则明确未形成机器源）。
2. 字段逐项记录名称或字段 ID、类型/引用、必填/可空/缺省、含义、范围/枚举/单位、长度或计数来源、条件有效性；不适用项按实际编码裁剪。复杂字段可用逐字段固定段落，不强制全角竖线分隔。
3. 跨字段不变量、身份/键、生产/消费与可见时点；拥有者、借用/复制、持久/删除及版本规则按对象实际性质填写。
4. 一条合法与一条拒绝或边界实例、原设计 V/Case 引用。字段阅读视图必须与固定机器源核对；别名和转换给逐字段映射及损失语义。

只引用已有公共对象时，不再复制上述字段，只给 ID、来源基线、本文使用/转换语义和定位链接。`design.data-dictionary` 的完整字典视图是机器源的受控投影，不产生第二套字段权威。

**系统公共错误码是公共数据结构的必填类别**。总体系统设计 §9（纯软件项目的软件系统设计 §8）应逐码登记稳定 Error ID/实际代码值、错误名称、精确定义和触发条件、错误载荷字段及其来源、结果是否已知、是否已有副作用、允许调用方采取的动作，以及适用范围与版本/兼容要求；不得只写“参数错误”“内部错误”或列一串名称。系统决定公共语义，已有机器 Error enum/Schema/catalog 是代码值、类型和编码的唯一机器权威；系统正文给同一基线的完整可读视图，发现两者冲突必须回到有权来源裁决。若尚无机器定义，标为 Proposed 并阻断把它宣称为可实现的公共契约。已有外部错误需明确映射到哪个系统错误码；仅模块内部使用的私有异常不冒充系统公共错误。

### 3.2 操作/接口记录

每个**由本文拥有的操作**至少记录：

1. 稳定成员 ID、功能与边界、提供/调用/消费方、真实签名或协议操作、同步/异步、固定来源及状态。
2. 输入参数与 request/event 类型引用；逐参数说明必填/默认、范围/编码/所有权、身份权限、前置与校验顺序。
3. 成功输出、异步通知或无返回的精确含义；分别列出 accepted、completed、effective、released 等适用时点，不以一个“成功”覆盖。
4. **逐条件**引用系统公共 Error ID/代码及其触发事实、优先级、已发生/可能副作用、调用方下一步；注明超时、取消、重复/幂等与并发边界。接口不得重新定义该码的含义；接口特有的私有错误须注明 owner 及是否映射为公共错误。
5. 按实际责任补充统计/日志/诊断口径、持久状态或表列影响、时序/资源/寄存器、兼容/弃用等扩展。无该责任时给**一次清晰的归属/不适用决定**，无需在每个操作重复写“数据库：无”。

§14.3 等**引用/分配视图**只写交接责任、方向、固定语义、适用错误及原成员 ID，不重述完整字段与签名。模块内部私有函数、ISD 函数实现和公共 wire 契约使用相同的“输入—结果—逐条件错误”逻辑，但固定字段可按责任范围扩展，不强迫同一六段标题。

### 3.3 系统公共错误码目录与下级承接

在总体系统设计 §9；纯软件项目在软件系统设计 §8，建立**完整的系统公共错误码目录**。不能只在接口错误分支、数据字典、代码 enum 或下级模块中零散出现。目录范围由系统对外行为与适用配置确定，包括拒绝、部分完成、结果未知及需要调用方动作的错误；内部私有异常可保留在下级，但跨系统边界前须明确映射。

每个错误码用一条固定记录，而不是把语义压入代码名：

```markdown
#### <Error ID> · <稳定代码值或机器来源>
- 定义与适用范围：<该码表示的唯一事实；哪些情况不属于此码>
- 触发条件与判定者：<具体条件、事实来源、产生责任单元>
- 结果与副作用：<未受理/已受理/结果未知/已完成；已发生或可能发生什么>
- 错误载荷：<类型 ID、必填字段、关联身份、敏感信息边界>
- 调用方动作：<修正输入/查询原状态/等待/安全重试/停止/人工处理及前提>
- 模块承接：<哪些模块产生、透传、转换、消费；转换目标 Error ID>
- 唯一来源与兼容：<机器 enum/Schema/catalog 的版本、selector/hash；或 Proposed 缺口>
- 验证：<触发向量、契约 Case、系统组合验收与未运行状态>
```

另在同一章节维护短的**Error ID → 下级模块/机制/接口成员 → 使用方式 → 验证项**承接矩阵，或引用能双向核对的等价项目清单。系统决定含义和公共动作；接口逐失败条件引用 Error ID；子系统/模块说明自己何时产生、如何透传、把哪个私有/外部错误映射过来，以及是否消费并采取动作；ISD 落到具体分支/symbol；测试证明代码值、载荷、状态和调用方动作一致。没有承接者、承接关系冲突或下级私自改含义，应阻止该范围被判为设计完成。

**不得把 RFC3339 毫秒 UTC 或 `?` 可空记法设为全局默认**：采用何种时间精度、时区及可空语法，应由实际机器契约或项目规范定义，并在阅读视图中一致呈现。

## 4. 逐模板必须显式提供的清单

以下清单须写到**每份模板对应章节的开头**：作者应描述哪些数据结构、哪些接口、写到什么层级，以及哪些对象只引用原 authority。只在共享指南放一张总表仍不够；模板生成的文档必须保留可填写入口或明确的 `N/A + 依据 + 实际归属`。不要求所有设计都拥有数据库或对外 API。

1. **总体系统设计 `design.system`：§9 数据、§10 接口。**
   - 数据：系统输入/输出、跨软件/固件/硬件边界交换的对象、描述符或控制/状态信息，以及影响容量、寿命和安全的共享数据；**逐项列出系统公共错误码与错误载荷**。§10.5 只登记这些公共结构及编码的同源入口。
   - 接口：系统外部与跨域接口，按数据面、控制/管理、维护/调试、配置/启动分类；列参与方、方向、内容、时序/确认、失败和唯一权威；每个失败出口引用 §9 的 Error ID，不能在接口节另造同义错误。
   - 深度：本级决定系统边界和共同保证；对关键字段给受控阅读视图或源引用。不能因系统较小就自动取得已有 Schema、RTL 或下级私有字段的 authority；也不能只给文档索引而不解释关键交接。

2. **软件系统设计 `design.software-system`：§8 数据、§9 接口。**
   - 数据：软件系统的业务输入/输出、跨子系统或直属模块共享的对象与状态、权威/缓存/持久形态、数据变换和保留清理边界；**逐项列出软件系统拥有的公共错误码与错误载荷**。若有上级总体系统，继承其代码和语义，只定义明确分配给本级的新码或映射。
   - 接口：用户/外部服务入口、软件系统对外 API、直属子系统/模块间的公共交接，以及维护入口；逐操作说明请求、结果、所引用的系统 Error ID、权限和生效边界。
   - 深度：完整决定系统级跨边界语义与责任；字段来自唯一机器契约或有标记的 Proposed 定义，不复制模块私有结构。

3. **系统机制设计 `design.system-mechanism`：§4 数据、§5 接口；§14.3 只做分配。**
   - 数据：机制身份、状态、消息/事件、阶段结果、判定事实、共享资源引用及其所有权、可见性和寿命。
   - 接口：参与方为推进、查询、取消、恢复和释放而交换的操作/事件，含前置、确认、重复、期限及异常出口。
   - 深度：§4/§5 是机制独有规则的唯一详细位置；§14.3 列提供/消费对象及原成员 ID，不复制 §5 的字段和完整签名。

4. **软件子系统设计 `design.subsystem`：§5 数据、§6 接口。**
   - 数据：子系统对外暴露的对象、状态与阶段形态，以及直属模块共享的身份、键、一致性、寿命和变换；列出继承的系统 Error ID、由哪个本层事件触发，及获准新增的本层公共错误。
   - 接口：子系统边界提供/消费的操作，及本层直属对象间由子系统负责的交接；逐项给双方、方向、主要输入/输出、所引用的 Error ID 和唯一维护位置。
   - 深度：本层共同规则可展开，模块私有数据/函数只引用其 owner；规格缺失与代码未实现分开登记。

5. **软件模块设计 `design.definition`：§6 数据、§9 接口；§5.3 文件间交接。**
   - 数据：本模块公共可复用类型、私有关键状态/工作区、键/枚举/DTO、字段约束、读写者、所有权及寿命。§6 对本文拥有的对象逐字段展开。
   - 接口：模块对外提供和消费的函数/API/消息，逐参数、互斥成功结果、逐条件引用的系统/子系统 Error ID、副作用、并发/超时和验证入口；明确本模块是产生、透传、转换还是消费每个错误，转换不得改变公共语义。§5.3 列实际文件间调用与 §9 成员的对应；真正私有的新交接才在本层另定义。
   - 深度：对外达到调用方可编码、可写用例；内部达到实现者能确定关键行为，不重复维护机器字段。

6. **数据字典 `design.data-dictionary`：§3–§6 数据；接口不在此定义。**
   - 数据：Entity/Object、逐字段含义、类型/范围/可空/单位、枚举/错误值、身份/键/引用、生产消费和 ownership；系统错误码可在此提供受控字典视图，但必须引用系统决定及机器代码来源，不重定含义；必要时补序列化、保留及迁移。
   - 接口：本模板不拥有操作接口；列出消费这些数据的接口成员 ID 和来源链接即可。
   - 深度：提供完整可读字段视图，但明确机器 Schema/IDL/DDL/ABI 与数据字典的 authority 分工；不能凭模板名称覆盖既有机器源。

7. **实现规格 ISD `design.implementation`：§4 数据、§5 函数与接口实现。**
   - 数据：文件/函数内私有类型、持久表映射、缓存、锁保护状态、参数到工作区的表示、创建/借用/释放与升级落点；公共结构仅引用模块/机器源 ID。
   - 接口：关键函数的真实签名、逐参数前提及校验位置、成功/错误返回、所有权、副作用、并发/事务/阻塞、实际调用和清理步骤；每个错误出口定位继承的 Error ID 或私有异常到公共错误的映射函数与验证。
   - 深度：代码级实现输入；不得在 ISD 新决定模块公共行为、对外错误或跨模块政策。已有模块设计兼作 ISD 时给覆盖映射，不另造重复文档。

8. **板卡设计 `design.hardware`：§3–§5 数据/信号与外部接口。**
   - 数据：板级控制/状态信号、连接器引脚定义、寄存器或配置字段中由板卡负责的部分，以及电压、电流、位宽、时钟/复位、方向和有效条件。
   - 接口：板间、器件、连接器、总线、电源/时钟/复位交接；列协议标准及版本、项目选项、电气/时序、保护和原理图/PCB 落点。
   - 深度：板级契约与实现约束；既有芯片数据手册、标准或 RTL/寄存器源仍是其字段 authority。需在模板中增设显眼的“数据/信号清单”入口，不能只靠接口宽表暗示。

9. **FPGA 程序设计 `design.fpga`：§2、§4–§5 数据/端口与协议。**
   - 数据：RTL 顶层及关键模块的端口/信号、CSR/register map、描述符/packet/stream 格式、状态与有效位、位宽/端序/复位值、clock domain 和寿命。
   - 接口：模块端口、Host/板卡/软件边界、握手/背压、顺序/确认、时序/跨时钟域及错误信号。
   - 深度：端口级可实现与可验证；沿用唯一 RTL/寄存器/接口源，模板需明确“数据格式与信号清单”入口，不以 §3 数据流图替代定义。

10. **接口契约 `contracts.specification`：§2–§5、§8。**
    - 数据：request、response、event、**系统错误码及错误载荷**对象的完整字段阅读视图、编码/范围、身份关联和版本来源；公共错误语义继承系统设计。
    - 接口：每个 Operation/Message/Event 的真实签名或 wire 形态、提供/消费、逐条件 Error ID、accepted/completed 等保证；另保留幂等/并发/事务和兼容/弃用规则。
    - 深度：公共契约级；机器定义拥有字段，正文解释共同语义。数据库物理列不是每个公共操作的默认交付项。

11. **接口控制 `interfaces.control`：§2、§4–§6、§9。**
    - 数据：双方交换的命令、状态、数据 Schema、寄存器/信号及编码，逐项绑定源、版本和 selector。
    - 接口：接口注册、物理/传输绑定、时序/状态、错误/timeout/重试/幂等、版本协商和双方实现责任。
    - 深度：双方可互操作的共同边界；完整字段阅读视图与机器源一致，不代替各方内部实现设计。

管理、需求、验证、运维等**非设计/契约模板**不强制增加独立的数据结构和接口章节；它们只引用实际需要的成员 ID 和权威来源。后续修改须对上述 11 类模板逐一做“数据入口、接口入口、适用性、唯一来源”四项检查，尤其不能遗漏硬件/FPGA，也不能让数据字典凭空定义操作。

## 5. 校验与验收建议

1. **来源与角色**：沿现有 `validate-interface-catalog` 检查成员 ID、request/response/event/error 角色、selector、版本/hash、完整阅读视图与覆盖范围；不新建并行目录。
2. **结构 lint**：`validate-design` 仅在模板声明“本节拥有完整数据/操作记录”时检查 ID、来源、字段/参数、成功和逐条件错误等结构槽位；引用视图检查 ID/基线，不要求重复全记录。`N/A` 必须有适用性依据或真实 owner，不能靠非空字符串通过。`｜` 可以是排版符号，不是唯一合法语法。
3. **纵向承接与错误码完整性**：以系统公共数据/接口成员及 Error ID 为独立分母，核对系统决定→机制/契约→子系统→模块→ISD→实现/测试的提供、消费、产生/透传/转换关系；系统 §9/软件 §8 中缺码、重复码、同码异义、接口引用不存在的码、下级悄然新增公共码或错误载荷/机器定义不一致都应报错。未实现/未验证保留在分母，不能靠删除下级行通过。
4. **领域反例**：至少覆盖缺失响应、request/response 互换、机器字段与阅读视图不一致、接口缺错误触发条件、复用对象重复定义、机制 §14.3 复写不同签名、硬件/FPGA 被误要求数据库、只读/无持久化接口被迫填表，以及只有空标题或模板占位符的文档。
5. **人工 Gate**：从一份实际输入推演提供方、消费方、成功及失败；检查数据来源、适用单位/编码、返回后的合法动作、实现与测试能否对齐。机械 PASS 不代表接口语义、代码或运行验证 PASS。
6. **版本与回归**：若模板字段要求改变，逐份提升受影响 Template Version；STD 统一发布版本单独递增。同步 catalog、规范入口、AI 指南和模板测试。冻结旧项目不自动迁移。

## 6. 推荐执行顺序与完成条件

1. 先修改唯一共享规范和两份教学案例：一个有机器 Schema/错误向量的软件接口，一个有真实信号/时序定义的硬件或 RTL 端口。案例须能从字段/操作追到实际来源和验证。
2. 再按第 4 节逐模板接入，**不改变既有目录结构**；先修模块、机制、数据字典与两份契约，再核对系统/子系统、ISD、硬件/FPGA。
3. 最后扩校验器和反例测试，逐模板跑生成、结构和来源映射回归；对旧格式给清晰迁移诊断，不静默将新格式套到未升级项目。

完成标准：作者能从正文理解行为，Agent 能沿稳定成员 ID 找到同一基线的全字段/签名、实现责任和验证；编码者无需猜输入、成功结果、错误条件或适用数据寿命；公共字段没有两份可独立修改的权威。尚未决定的设计被明确标为缺口，不被格式校验的 PASS 掩盖。
