# 接口、契约与数据 AI 编写指南

版本：0.3.0-draft.1 · 日期：2026-09-12 · 状态：方法草案，待实际写作任务验证

主模板：`interfaces.control`、`contracts.specification`、`design.data-dictionary`。
先读[通用 AI 编写指南](../ai-authoring-guide.md)，再读项目已采用的对应模板。本文补充本类型的工作方法，不重复公共来源、权限和状态规则，不自动升级项目模板。

## 1. 输入与边界

固定生产/提供方、消费方、共同能力、上级 Constraint/Process、协议版本、现有机器定义及双方实际设计。公共字段与行为必须有唯一 authority；先判断是引用既有完整定义、整合局部定义、解决冲突，还是确需共同设计，不默认新增一套 API 或配置路径。

接口控制说明交接边界；契约规格定义公共操作与行为；数据字典定义对象和字段语义。三者可以引用同一 Schema/OpenAPI/RTL 定义，但不能各复制一份产生分歧。正文仍应解释读者必需的工作原理，不能只有机器文件索引。

## 2. 专项执行顺序

1. 沿一次能力的端到端过程列实际交接与责任方，确认地址/标识/实例身份和配置作用域。
2. 逐操作定义输入前提、授权、准入/拒绝、结果语义、错误及可见时点；接收、完成、生效、持久化不混用。
3. 逐对象定义身份、版本、单位、范围、生产/消费、所有权和生命周期；复制、引用与转换分别说明。
4. 推演部分失败、超时、重试、迟到、重复和版本不匹配；有副作用时核实旧执行隔离与幂等/去重条件，不把超时等于未执行。
5. 对照双方实际字段/行为逐项收口，给出共同向量、兼容性规则及本地/组合验收；共同语义未定不能留给两方独立猜测。

6. 按[映射规范](../interface-data-mapping-standard.md)建立现有接口根的人读/机器目录，固定来源 selector、version/revision/hash 和正文 Document ID/稳定锚点；Document ID/version 复用封面与同名 metadata 并与目录核对，不新增隐藏身份；先确认实际定义再标完整。
7. 用同一类型 ID 解析 request/response/event/field，对照源签名检查每个适用角色、selector 和目标类型恰好出现一次；反向检查目录无伪造槽位，不强加单向操作没有的响应。已有共享类型不重定义；投影/别名不能替代真实签名。修改成员保留身份，逐项比较旧消费基线、废弃/替代和兼容，不能只升级版本字符串。
8. 用[完整案例](../examples/interfaces/README.md)的方法实际核对字段、签名、枚举/错误、默认/null、长度/计数，并分别记录原生解析器的检查范围。目录定位通过不代表 CLI/信号或 wire ABI 校验。

## 3. 不同模板的实际产物

| Template ID | 工作重点 | 不得遗漏 |
|---|---|---|
| interfaces.control | 跨单元接口及物理/传输/逻辑边界、责任和运行条件 | 方向、提供/消费方、实际协议/接口 authority、版本依赖 |
| contracts.specification | 公共操作、状态、错误、一致性、权限和兼容行为 | 确认语义、部分失败、重复执行、恢复与验收条件 |
| design.data-dictionary | 实体、字段、类型、单位、枚举、约束和关系 | 缺省/缺失/空值差异、编码/序列化及生命周期 |

底层布局只在实际需要时展开顺序、大小、对齐和端序，不给逻辑 JSON 对象虚构内存 ABI。机器 Schema 能检查类型不代表表达了行为，错误与状态转换仍需共同定义；已有完整行为规格则引用它而不再次维护。

## 4. 图例与写法示例

数据变化可复用[机制对象图](../../templates/diagrams/mechanism/objects.svg)，操作时序可用[机制时序图](../../templates/diagrams/mechanism/sequence.svg)。先读[机制图形说明](../../templates/diagrams/mechanism/README.md)，仅复用图式，不继承 EX-OBS-01 的字段或只读边界。

虚构例：弱写法“发送成功即配置更新成功”。先查“成功”只是入队、持久化还是已由全部参与方应用；若存在多个阶段，就定义各阶段可观察状态、返回值、部分失败和后续查询方式。两端必须对同一生效点达成共同定义，不能让客户端和设备各解释一次。

若业务只需要单阶段确认，不为完整模板新增任务队列；按真实条件给出最小自洽语义。

接口控制/契约规格工作稿 0.3.0 已给每章段落式建议和完成条件；需要硬件交接时，以
[Host—驱动—FPGA 完整教学路径](../examples/host-fpga-transfer-example.md)串起边界、原生布局、
操作、失败及验证。只有实际需要的二进制字段才定义 offset；原生格式的语义检查仍用其工具，
不能用目录索引或 C 编译通过替代真实总线/RTL/隔离验证。

先从已有设计/采用分配固定逐成员承接范围，再填写目录中的 downstream。catalog 2.0.0
通过 family.downstream_inventory 引用独立源，按 role/module/backend 双向比较；不适用须
逐项说明，未实现不等于不适用。清单本身遗漏真实消费者仍需内容复审，不能倒过来从通过行生成分母。

### 4.1 失败位置、发现方法与合法修正

| 具体错配 | 怎样发现 | 怎样修正 |
|---|---|---|
| 正文 OP01 存在，catalog 未收录；或源仅 TODO | 按全部入口清单、source inventory 双向比对并实际解析 selector | 补真实定义/索引或保留未完成，不能删分母 |
| 两操作复用 OP01；章节移动使 contract-data 锚点失效 | 重复 ID / prose 定位回归 | 恢复稳定身份/显式锚点，移址只更新路径 |
| 请求引用 Unknown；两模块各造不同 Shared | $ref/binding 解析、来源唯一性及逐字段比较 | 引用原类型 ID；必要转换另记映射，不覆盖旧源 |
| request/response 角色对调，或 response 漏项/重复 | 从源签名生成角色/selector 分母，与目录双向比对再核目标类型 | 恢复完整且唯一的实际映射；未知布局须用项目解析器，不放行为空 |
| 封面新版本，metadata/目录或旧隐藏注释仍是旧版 | 复用原封面校验，再核 metadata 与目录，显式注入漂移反例 | 同步现有身份；删除冗余注释或保持一致，不建立第二个版本来源 |
| 正文 u32、源 u64；count、枚举、错误、default/null 不同 | 完整投影逐字校验 + 边界向量 | 原 authority 决定语义，再同次更新视图和消费基线 |
| 逻辑对象标成 wire ABI，用 sizeof 推断长度 | 编码/布局内容复审和实际序列化向量 | 分开逻辑/Host/wire，只对实际 ABI 规定偏移 |
| 新增字段但 consumer 仍旧 revision/hash | 下游基线匹配、旧新向量对照 | 按项目采用决定迁移，不能只改标签掩盖实现差异 |
| 一个 backend PASS 就汇总全 PASS，移除 NOT_RUN/未承接 Case | 逐 backend 检查 + 固定范围清单/验证表复审 | 保留未完成分母，真实结果分别报告 |

自动检查的覆盖与未覆盖项以工具输出为准；语义兼容、未被清单发现的接口与 Run 真实性仍需内容/项目验证。

## 5. 完成检查与下游承接

对每个公共操作/对象找到实际提供与消费方、唯一版本来源、正常/失败结果和双方测试位置；用同一向量比较两方解释，差异必须处理或明确待决。仅链接存在、Schema PASS 或单方自测不足以证明共同语义闭合。

接口语义、错误和向量评审后可以并行实现；各方契约测试后进入集成，组合验证后才关闭能力目标。交付共同保证及内部自由度，不自行升级文档批准或运行许可。
