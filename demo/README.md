# STD 示例索引

本目录保存经明确授权、可用于验证 STD 模板与编写方法的示例。示例展示的是具体项目在特定
commit 上的设计结果，不自动升级为 STD 的规范性正文，也不证明其符合后来发布的模板版本。

| 示例 | 用途 | authority 与状态 |
|---|---|---|
| [LLMTier 软件模块设计](llmtier-module-design/README.md) | HTTP API 服务模块和 Web UI 模块的实际写法；用于检验操作面、内部组成、文件接口、数据字段和实现步骤的表达 | LLMTier 项目 authority；源文档为 Draft，采用 `design.definition` `2.3.0` |

目录中的其他本地参考材料如带有保密或授权边界，应继续按其原限制使用；未列入本索引不表示
获得复制、发布或训练授权。
